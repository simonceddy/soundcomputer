import { createSlice } from '@reduxjs/toolkit';

export const instrumentsSlice = createSlice({
  name: 'instruments',
  initialState: {},
  reducers: {

  },
});

// export const { } = instrumentsSlice.actions;

export default instrumentsSlice.reducer;
