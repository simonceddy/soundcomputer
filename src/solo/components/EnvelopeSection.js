function EnvelopeSection({ children }) {
  return (
    <div className="col justify-start relative items-center rounded-lg bg-blue-200/50 m-1">
      {children}
    </div>
  );
}

export default EnvelopeSection;
