import { createSlice } from '@reduxjs/toolkit';

export const effectsSlice = createSlice({
  name: 'effects',
  initialState: {},
  reducers: {

  },
});

// export const { } = effectsSlice.actions;

export default effectsSlice.reducer;
