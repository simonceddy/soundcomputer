/* eslint-disable no-unused-vars */
/* eslint-disable prefer-const */

// Moz noise
let noiseDuration = 1;
let bandHz = 1000;

// Moz sweep
let attackTime = 0.2;
let releaseTime = 0.5;
const sweepLength = 2;

// Moz sample
let playbackRate = 1;

// Moz pulse
let pulseHz = 880;
let lfoHz = 30;
const pulseTime = 1;
